Is the target variable a `1` or a `0`? When we look at the `value_counts()` of the [[targets]], what is the distribution of binary values?

#valuecounts
#classification 
#targets 
#binaryclassificationproblem
